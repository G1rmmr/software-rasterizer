#pragma once

#include "Math.h"

inline math::Matrix4 CreatePerspective(float fov, float aspect, float n, float f) {
}

inline math::Matrix4 CreateLookAt(
    const math::Vector3& eye,
    const math::Vector3& target,
    const math::Vector3& up) {
}

inline math::
