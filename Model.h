#pragma once

#include <array>

#include "Graphics.h"

namespace model{
    constexpr static inline math::Vector3 POS(0.f, 0.f, 0.5f);

    static inline std::array<graphics::Vertex, 3> Vertices = {};
    
    static inline void Draw(){
        
    }
}
